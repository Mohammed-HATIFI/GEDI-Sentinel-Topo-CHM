# Model archive

Checkpoints are not committed to GitHub. `manifest.json` records final parameters and SHA-256 identities. Store binaries in a versioned research archive and replace `archive_url` before release.
